7 categories ranked by impact:

1. **CRITICAL**: Core Concepts (`core-concepts`) - layers, dependency rule, inward-pointing dependencies
2. **CRITICAL**: Ports and Adapters (`ports-and-adapters`) - port interfaces, adapter implementations, primary/secondary adapters
3. **CRITICAL**: Domain Model (`domain-model`) - entities, value objects, domain services
4. **HIGH**: Use Cases (`use-cases`) - application layer orchestration, thin use cases
5. **HIGH**: Dependency Injection (`dependency-injection`) - composition root, constructor injection
6. **MEDIUM**: Testing Strategy (`testing-strategy`) - layer-specific testing, mocking strategy
7. **MEDIUM**: Common Mistakes (`common-mistakes`) - premature abstraction, anemic domain, framework leaks
