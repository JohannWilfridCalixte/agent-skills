7 categories ranked by impact:

1. **CRITICAL**: Core Principles (`core-principles`) - foundational type safety: end-to-end typesafety, narrow types, maximum inference
2. **CRITICAL**: Types (`types`) - dataclass vs Pydantic vs TypedDict vs NamedTuple, immutability, generics, Protocol, TypeIs, schemas, return types
3. **CRITICAL**: Match Exhaustiveness (`match-exhaustiveness`) - union/enum exhaustiveness via assert_never
4. **HIGH**: Error Handling (`error-handling`) - Result pattern, no raising for expected failures, exception rules
5. **HIGH**: Code Style (`code-style`) - naming, spacing, guard clauses, nesting depth
6. **MEDIUM**: Imports (`imports`) - ordering, grouping, TYPE_CHECKING, absolute imports
7. **MEDIUM**: Patterns (`patterns`) - strategy pattern, common anti-patterns
